import { TOOLCHAIN_READY } from './placeholder'

// Minimal Vite entry stub — React UI lands in task 2.2.
if (import.meta.env.DEV) {
  console.debug('[TheGitDown] toolchain ready:', TOOLCHAIN_READY)
}
